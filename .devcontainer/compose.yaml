services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    ipc: "host"
    networks:
      - devnet

networks:
  devnet:
