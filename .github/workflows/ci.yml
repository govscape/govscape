defaults:
      run:
        shell: bash -el {0}

name: CI
jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest]
        python: ['3.11.14']
      fail-fast: false
    runs-on: ${{ matrix.os }}
    container:
      # TODO, once first PR is merged, update to remove the branch from the version tag
      image: ghcr.io/bcglee/govscape-base:py3.11.14-lucene10.0.0-poetry2.3.2-lucene-keyword-index
    env:
      POETRY_VIRTUALENVS_CREATE: false
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v5
      - name: Install packages
        run: |
          /opt/poetry/bin/poetry install
      - name: Run tests
        run: |
          /opt/poetry/bin/poetry run pytest govscape/tests
      - uses: codecov/codecov-action@v5

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
