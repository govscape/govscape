defaults:
      run:
        shell: bash -el {0}

name: CI
jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest]
        python: ['3.11.14']
      fail-fast: false
    runs-on: ${{ matrix.os }}
    container:
      # TODO, once first PR is merged, update to use proper version tag instead of sha for the base image
      image: ghcr.io/bcglee/govscape-base:sha-2c5d3a9
    env:
      POETRY_VIRTUALENVS_CREATE: false
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v5
      - name: Install Poetry
        uses: snok/install-poetry@v1
      - name: Install package
        run: |
          /github/home/.local/bin/poetry install
      - name: Run tests
        run: |
          /github/home/.local/bin/poetry run pytest govscape/tests
      - uses: codecov/codecov-action@v5

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
